apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: myapp
  labels:
    app: backend
data:
  db.host: "database.myapp.svc.cluster.local"
  db.port: "5432"
  log.level: "info"
  app.name: "backend-api"
